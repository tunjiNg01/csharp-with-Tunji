// See https://aka.ms/new-console-template for more information
Console.WriteLine("Hello, World!");

// Single dimensional array
int[] frequency = new int[6] { 1, 2, 3, 4, 5, 5 };


// avoid using the new keyword if the is been
// initialised immediatel
int[] number = { 1, 3, 4, 5, 6 };

// the type of an array is developed is infered from
// the indicated during the declaration if the type is a
// reference type it array of the type with each set item to null by default.

//MULTIDIMENSION ARRAY

int[,] ad = new int[2,3] { { 1, 0, 2}, { 1, 2, 3} };
